{
  "name": "x",
  "version": "0.3.4",
  "main": "main.ts",
  "repository": "https://github.com/0x-jerry/xx.git",
  "author": "0x-jerry <x.jerry.wang@gmail.com>",
  "license": "MIT",
  "scripts": {
    "deno": "deno run --import-map ./import_maps.json -A --unstable",
    "dev": "deno run --import-map ./import_maps.json -A --no-check --unstable x.ts",
    "release": "x flow release",
    "test": "deno test --import-map ./import_maps.json --allow-env --allow-read --allow-run --unstable"
  }
}