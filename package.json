{
  "name": "x",
  "version": "0.2.7",
  "main": "main.ts",
  "repository": "https://github.com/0x-jerry/xx.git",
  "author": "0x-jerry <x.jerry.wang@gmail.com>",
  "license": "MIT",
  "scripts": {
    "deno": "deno run --import-map ./import_maps.json -A --no-check",
    "dev": "deno run --import-map ./import_maps.json -A --no-check x.ts",
    "release": "deno run --import-map ./import_maps.json -A --unstable scripts/release.ts",
    "test": "deno test --import-map ./import_maps.json --allow-env --allow-read --allow-run --unstable"
  }
}